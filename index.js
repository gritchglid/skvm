const installer = require("./lib/installer")
const path_manager = require("./lib/path_manager")
const versions = require("./lib/versions")

module.exports = {
  installer,
  path_manager,
  versions
}
